# venv
.venv/
venv/

# local data store (regenerated by ingestion) — anchored to repo root so we
# don't accidentally ignore the graphsignal/data/ Python package.
/data/

# spec / planning docs (kept local, not committed)
MDs/

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
